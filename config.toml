baseURL = "https://charlibot.github.io/"
languageCode = "en-gb"
title = "charlibot"
theme = "noteworthy"

summaryLength = 40
# Enable for syntax highlighting
pygmentsUseClasses = true


[params]
  # Blog description at the top of the homepage. Supports markdown.
  description = "Notes on programming."
  github = "https://github.com/charlibot"
[[menu.main]]
	identifier = "about"
	name = "About"
	url = "/about/"
	weight = 1 # Weight is an integer used to sort the menu items. The sorting goes from smallest to largest numbers. If weight is not defined for each menu entry, Hugo will sort the entries alphabetically.

[[menu.main]]
	identifier = "tags"
	name = "Tags"
	url = "/tags/"
	weight = 2

[[menu.main]]
	name = "Archives"
	identifier = "archives"
	url = "/archives/"
	weight = 3

[markup]
  [markup.highlight]
    style = "monokai"

